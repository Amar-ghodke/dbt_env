semantic_models:
  - name: employee # A semantic model with the name Transactions
    model: ref('EMP_DEP') # References the dbt model named `fact_transactions`
    description: "EMP_DEP table contains all the data of employee, dept and managers."
    defaults:
      agg_time_dimension: none

    entities: # Entities included in the table are defined here. MetricFlow will use these columns as join keys.
      - name: employeeid
        type: primary
        expr: EMPLOYEEID
      - name: deptid
        type: foreign
        expr: DEPTID
      - name: mng_id
        type: natural
        expr: MNG_ID

    dimensions: # dimensions are qualitative values such as names, dates, or geographical data. They provide context to metrics and allow "metric by group" data slicing.
      - name: deptid
        type: categorical
        
      - name: mng_id
        type: categorical
        expr: mng_id

    measures: # Measures are columns we perform an aggregation over. Measures are inputs to metrics.
      - name: employee_total
        description: "The total no of the employee."
        agg: sum

      - name: sales
        description: "The total sale of the transaction."
        agg: sum
        expr: transaction_total

      - name: median_sales
        description: "The median sale of the transaction."
        agg: median
        expr: transaction_total

  - name: account_target # Another semantic model called customers.
    model: ref('sql_join')
    description: "Google sheet and target join."

    entities:
      - name: customer
        type: primary
        expr: customer_id

    dimensions:
      - name: first_name
        type: categorical