version: 2

sources:
  - name: GOOGLE_SHEETS
    database: sndbx_db
    freshness: # default freshness  
    schema: GOOGLE_SHEETS
    tables:
      - name: GOOGLE_SHEETS
      - name: Target
      
  - name: s3
    database: sndbx_db
    freshness: # default freshness  
    schema: s3
    tables:
      -name: CALL_DETAIL